# Database (PostgreSQL + PostGIS)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=neartalk
DB_USERNAME=neartalk
DB_PASSWORD=neartalk

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-secret-key-must-be-at-least-32-characters-long

# AWS S3 (or Cloudflare R2)
S3_BUCKET=neartalk-dev
S3_REGION=ap-northeast-2
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
S3_ENDPOINT=
